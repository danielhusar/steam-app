'use strict';

var escapeStringRegexp = require('escape-string-regexp');

module.exports = function (app) {
	app.service('filterItems', function () {
		return function (data, game, items) {

		};
	});
};
